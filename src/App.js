import React from "react";
//import './App.css';
function App() {
  return (
    <React.Fragment>
{/*       <TodoCounter />
      <TodoSearch />
      <CreateTodoButton/>
      <TodoList>
        {todos.map(todo => (
          <TodoItem key={todo.text} text={todo.text}/>
        ))}
      </TodoList> */}
    </React.Fragment>
  );
}

export default App;